# Names the Gemini TTS is known to mispronounce, and the speakable spelling to
# write instead. The writer consults this while scripting; check_episode.py warns
# (never fails) when a raw form appears in the script.
#
#   <raw form as normally written>: "<speakable spelling to write in the script>"
#
# Populated over time from feedback.md notes (the writer may add entries itself).
# Example:
#   Aramco: "ah-RAHM-koh"

{}
