# Listener interest weights — the daily-ai-podcast writer reads this nightly
# (step 1.5) and applies it when choosing dives vs sweep lines.
#
# Edit freely, one line per preference; plain language beats taxonomy. The writer
# may also append here when a feedback.md note reveals a durable preference.
#
# boost:      beats that win ties for dive slots
# downweight: beats that stay in the sweep unless the who-cares case is unusually strong

boost: []

downweight:
  - "embodied AI / robotics / VLA models — keep me aware via sweep lines and the
    occasional teaching moment, but a dive needs an unusually strong who-cares case"
